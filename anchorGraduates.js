/* siakan bahan */

let nama = 'Noel';
let nilai = 80;
let absen = 5;

/* algoritma nilai */
    if(nilai >= 70 && absen < 5){
        console.log(`${nama} mendapat ${nilai} dan absenya ${absen} jadi ${nama} dinyatakan lulus`)
    } else {

        console.log(`maaf ${nama} tidak lulus`);
    }

/* psendocode
INPUT "nama" with any string 
INPUT "nilai" with any number 
INPUT "absen" with any number

READ "nama", "nilai","absen"
IF nilai > 70 AND absen < 5
DISPLAY "nama" "lulus"
ElSE DISPLAY "nama" "tidak lulus"
*/
